# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Eventually' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Eventually
  pod “SwiftyJSON”
  pod "Alamofire"

  target 'EventuallyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EventuallyUITests' do
    # Pods for testing
  end

end
